import SwiftUI

enum AppTheme {
    static let accentColor: Color = .green          // used by tab/controls
    static let tileBackground: Color = Color(.secondarySystemBackground)
    static let tileCornerRadius: CGFloat = 18
}
